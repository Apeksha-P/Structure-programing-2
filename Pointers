#include <stdio.h>

sum(char name[],int size){
    int sum = 0;
    for(int i=0;i<size;i++){
        sum =sum + name[i];
    }
    printf("%d",sum);
}
int main(){
    char arr[]={'K','A','M','A','L'};
    int size = sizeof(arr) / sizeof(arr[0]);
    sum(arr,size);
    return 0;
}
